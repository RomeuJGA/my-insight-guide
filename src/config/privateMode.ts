/**
 * Private Access Gate
 * --------------------
 * Set `isPrivateMode = false` to disable the gate everywhere.
 * Change `GATE_PASSWORD` to rotate the access password.
 * Bypass via URL: append `?preview=1` to any URL.
 */
export const isPrivateMode = true;
export const GATE_PASSWORD = "ROMEUTEST123";
export const GATE_STORAGE_KEY = "intus_gate_access_v1";
export const GATE_PREVIEW_PARAM = "preview";
